#include "display.h"

// Stub (#27): the speech engine has no status UI yet — this only exists so
// the shared wslcd/gfx.cpp path still links. speech.md's Open Question #4
// (current phoneme + a formant-space plot) is deferred until the sequencer
// lands.
void display_init() {}
void display_task() {}
void display_bringup_test() {}
