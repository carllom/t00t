#include "display.h"

// Stub (#13): the tracker has no status UI yet — this only exists so the
// shared wslcd/gfx.cpp path still links. A real display (playback position,
// pattern/row, CPU load) lands with the pattern player.
void display_init() {}
void display_task() {}
void display_bringup_test() {}
