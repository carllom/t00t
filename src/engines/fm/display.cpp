#include "display.h"

// Stub: the FM engine has no status UI yet -- this only exists so the
// shared wslcd/gfx.cpp path still links. See module_fm.md §6.1 for the
// planned display.
void display_init() {}
void display_task() {}
void display_bringup_test() {}
